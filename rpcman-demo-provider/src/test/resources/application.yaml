server:
  port: 8080
  useNetty: false # open port = server.port + 1000


rpcman:
  zkServer: localhost:2182
  zkRoot: rpcman

app:
  id: app1
  namespace: public
  env: dev
  version: 1.0
  metas: "{dc: 'bk', gray: 'false', unit: 'B001'}"

logging:
  level:
    root: error
    cn.ipman.rpcman: info